#include "Simulator.h"

void Simulator::Simulate(long till)
{
    if(now == start)
    {
        Initialize();
    }

    for(now; now != )

    while(now != end)
    {
        now.Add(1, unit);
        
        for()
        {

        }
    }
}